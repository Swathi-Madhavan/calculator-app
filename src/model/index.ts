export interface AppColorsProp {
  calcBackColor: string;
  bodyBackColor: string;
  whiteColor: string;
  grayTextColor: string;
  blueTextColor: string;
  grayButtonColor: string;
  blueButtonColor: string;
  blackButtonColor: string;
}

export interface CustomButtonProps {
  
}